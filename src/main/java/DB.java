import java.util.ArrayList;

/**
 * Just a static class that simulates the "database" for this server. Stores messages.
 */
public class DB {
    public static ArrayList<Message> messages = new ArrayList<Message>();
}
